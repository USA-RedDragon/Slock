{
  "name": "shhlack",
  "version": "1.0.9",
  "description": "Shhlack is an encryptor/decryptor for peer to peer messages in Slack.It relies on pre shared keys between the peers so only the peers with the right passphrase will be able to see the encrypted message.",
  "main": "build.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "lint": "eslint '**/*.js'",
    "lint:fix": "npm run lint -- --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:mindedsecurity/shhlack.git"
  },
  "author": "Minded Security",
  "license": "ISC",
  "dependencies": {
    "browserify": "^16.2.3",
    "cross-spawn": "^6.0.5",
    "eslint": "^5.16.0",
    "eslint-config-google": "^0.13.0",
    "uglify-js": "^3.6.0"
  }
}
